spring:
  datasource:
    url: jdbc:mysql://localhost:3306/relay?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: ${DB_PASSWORD:sanjay@1083}
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

relay:
  auth:
    frontend-url: http://localhost:5173
    refresh-cookie-secure: false
  cors:
    allowed-origins: http://localhost:5173
  storage:
    endpoint: http://localhost:9000
    access-key: relay_minio
    secret-key: relay_minio_secret
    bucket: relay-files
    region: us-east-1

logging:
  level:
    com.relay: DEBUG
    org.hibernate.SQL: DEBUG
